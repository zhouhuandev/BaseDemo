apply from: "../lib.build.gradle"

android {
    //@Parcelize标记需要设置experimental = true
    androidExtensions {
        experimental = true
    }
}

dependencies {
    api deps.moshi
    kapt deps.moshi_kotlin_codegen
}
